<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:compatibility="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <!--<item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />-->

    <item android:id="@+id/impNotes" android:title="Important Notes"
        android:orderInCategory="101" app:showAsAction="never" />
    <item android:id="@+id/settings_menu" android:title="Settings"
        android:orderInCategory="100" app:showAsAction="never" />

    <item
        android:title="tNxt"
        android:id="@+id/menuNext"
        compatibility:showAsAction="ifRoom"
        compatibility:actionViewClass="android.widget.TextView" />

    <item android:id="@+id/break_timer"
        android:title="time"
        compatibility:showAsAction="ifRoom"
        compatibility:actionViewClass="android.widget.TextView" />

</menu>
